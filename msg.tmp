fix: 窗口标题被会话名覆盖、托盘无图标、更新用模态弹窗打断使用

用户实测反馈，三个都是"桌面客户端基本功"层面的问题：

1. 窗口标题显示会话名，而不是应用名

   Electron 默认让页面的 document.title 覆盖窗口标题，而 dsh 的 Web UI 会把
   标题设成当前会话名 —— 用户看到的就是会话名。
   修法：监听 page-title-updated 并 preventDefault，再显式
   setTitle('DSH-PX ' + 版本)。
   这是桌面应用该有的语义：窗口标题标识应用，不是标识当前文档。

2. 托盘图标仍然不是鲸鱼娘

   上一版用 app.getFileIcon(process.execPath) 从可执行文件提取，并且只验证了
   "exe 里有图标"就宣布修好 —— 验证了前提、没验证结果，这是本次反复出现的
   错误类型。实际行为是：开发态 execPath 是 electron.exe，取到的是 Electron
   默认图标；而且"依赖从 exe 提取"本身就是脆的设计。

   修法：改为自带专用托盘图标资源（桌面应用通行做法）：
   - build-icon.mjs 额外产出 tray-16.png / tray-32.png
   - 经 extraResources 放进 resources/，运行期直接读
   - 退回 build/ 目录（开发态）→ exe 图标 → 内存绘制
   实测日志确认走的是托盘资源。

3. 发现新版本用模态对话框，打断使用

   用户指出 Cursor / Codex 这类成熟产品不会用模态框打断工作，这是对的。
   模态框会夺走焦点、挡住界面，且必须处理掉才能继续 —— 对一个每天开着的
   客户端是明显倒退。

   改为：
   - 后台静默下载，不打断任何操作
   - 托盘菜单实时显示进度与状态，并按需重建（菜单是快照，改状态要重设）
   - 下载完成后发系统通知（不夺焦点），托盘菜单把"重启并安装"提到最前
   - 退出应用时自动安装
   - 只有用户主动点"手动检查更新"且已是最新时，才给一个反馈弹窗
     （那是用户发起的动作，需要回执；后台检查不需要）

附带整理 .gitignore：此前用 PowerShell Add-Content 追加中文导致编码损坏、
出现乱码行；改为用 Node 以 UTF-8 重写，并明确「素材不入库、成品托盘图标入库」
（托盘图标是运行期资源，不能依赖构建机重新生成）。

实测：开发态启动日志确认托盘图标取自 build/tray-32.png；语法检查通过。
